Commit changes

launchctl remove com.apple.CoreSimulator.CoreSimulatorService || true; \
git tag 1.5.9; \
git push --tags; \
pod trunk push PinLayout.podspec --allow-warnings

If no session exists: pod trunk register ldion@mirego.com 'Luc Dion' --description='macbook pro'

Pod

Swift Package Manager
===============================
Publish a new release:
	git tag 1.0.14
	git push origin --tags

	

Verifications: 
	pod update
	carthage update
	rm -rf .build/ && rm Package.pins; swift package show-dependencies --format json && swift package show-dependencies --format json